<task>
你需要协助我完成项目`FlowER: Flower Embodied Robotics`的WebApp。这是一个“为植物创造一副能够表达自我的身体”的机械臂项目。
<functions>
- 首页：导航页，有功能块指向二级页面入口`花朵视界`、`花朵日记`、与<我的花朵`小兰`>
- /花朵视界：调用API视频流，直播`花朵摄像头动态`
- /花朵日记：定时15min从API中调用花朵状态图片，调用多模态LLM记录在`现在……`卡片中；每1h将积累的`现在……`卡片内容调用模型总结压缩，记录在`最近`中并清除近1h的所有`现在……`；每天总结`最近`为一篇花朵日记，永久保存。本页面仅显示昨日日记，所有日记卡片在三级页面中。
- /<我的花朵> 花朵卡片，现在暂定名称为`小兰`，支持重命名，卡片紫色系渐变色，通过调用API显示花朵`当前心情`、`思想`（在头像旁边通过气泡显示，模型随机生成）、`生理数据`。
</functions>
<tech>
- 使用Python flask 构建项目
- 设计采用极简主义 / 现代主义设计，默认浅色模式；采用浅绿色系作为主题色
- 前端注重交互动效
- 运行在5000端口上
- 需要做移动端适配
</task>
<background>
- 你运行在Windows11上，先在本机完成demo部署。
- MVP思想，实现大于完美。
- 使用uv创建并管理Python虚拟环境。
- 将要删除的文件移动到本文件夹的`.temp`目录下，而非使用`rm`指令。
</background>